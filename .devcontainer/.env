# .devcontainer/.env
# Loaded automatically by docker compose before interpolation.
# COMPOSE_PROJECT_NAME sets the compose project name and is used to scope
# image tags and shared host paths to this project.
COMPOSE_PROJECT_NAME=symphony-maestro
