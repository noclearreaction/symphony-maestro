# .taskfiles/Taskfile-task.yaml
# Health tasks for the go-task runner.
version: "3"

tasks:
  doctor:
    desc: "Verify task runner is present and working"
    cmds:
      - task --version >/dev/null
    silent: true
