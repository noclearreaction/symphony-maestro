# .taskfiles/Taskfile-go.yaml
# Health tasks for the Go toolchain.
version: "3"

tasks:
  doctor:
    desc: "Verify go is present and working"
    cmds:
      - go version >/dev/null
    silent: true
